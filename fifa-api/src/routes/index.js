const playersRouter = require('./playersRouter')
const usersRouter = require('./usersRouter')
const loginRouter = require('./loginRouter')

module.exports = { playersRouter, usersRouter, loginRouter }
